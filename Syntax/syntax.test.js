describe('math function', () => {

    it('add two number', () => {
        expect(3 - 2).toBe(1);
    })


    test('plus', () => {
        const a = 20
        const b = 30
        expect(a + b).toBe(50)
    })
})